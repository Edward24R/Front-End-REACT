import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

/**
 * Index file for rendering application and enable ReactDom for navigation between pages.
 */

ReactDOM.render(
<React.StrictMode>
        <App /> 
</React.StrictMode>,
    document.getElementById('root'));


